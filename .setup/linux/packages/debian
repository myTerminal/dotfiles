#!/usr/bin/env bash

tw_log "Installing core packages..."
tw_execute sudo apt install \
           tmux \
           fish \
           fd-find \
           exa \
           batcat \
           ncdu \
           ripgrep \
           fzf \
           emacs \
           aspell \
           aspell-en \
           tig \
           ranger \
           xarchiver \
           htop \
           udisks2 \
           network-manager \
           dnsutils \
           docker \
           npm \
           brightnessctl \
           acpi \
           neofetch \
           wget \
           ssh

tw_log "Installing graphical packages..."
tw_execute sudo apt install \
           compton \
           i3 \
           xfce4 \
           feh \
           rofi \
           kitty \
           udiskie \
           gparted \
           thunar \
           gvfs \
           keepassxc \
           evolution \
           transmission \
           libreoffice \
           blueman \
           shotwell \
           gimp \
           darktable \
           clementine \
           mpg123 \
           pavucontrol \
           pavucontrol-qt \
           pulseaudio \
           pulseeffects \
           audacity \
           easytag \
           vlc \
           handbrake \
           blender \
           youtube-dl \
           kodi \
           flameshot \
           obs-studio \
           peek \
           fonts-inconsolata \
           fonts-liberation \
           fonts-roboto \
           fonts-firacode \
           fonts-font-awesome

# TODO: Fix for Debian: find a way to install the below graphical packages
tw_log "The following packages might need more attention before install:"
tw_log "virtualbox"

# TODO: Fix for Debian: find a way to install the below graphical packages
tw_log "The following packages were not found on Debian archives:"
tw_log "gstreamer, pulseaudio-bluetooth, fonts-opensans, fonts-roboto-mono, fonts-droid"

tw_log "Installing more packages..."
tw_execute sudo apt install \
           obsidian-icon-theme \
           syncthing-gtk \
           numlockx

# TODO: Fix for Debian: find a way to install the below graphical packages
tw_log "The following packages were not found on Debian archives:"
tw_log "gtk-theme-shades-of-grey, bibata-cursor-theme, google-chrome, tunnelbear, rememberthemilk, dynalist, pcloud, key-mon, gamehub, balena-etcher, nomachine, edex-ui"

# TODO: Fix for Debian: picom is here compton
# TODO: Fix for Debian: dmenu is here i3-dmenu-desktop
# TODO: Fix for Debian: need to replace firefox with firefox-developer-edition

tw_log "Installing packages through snap..."
tw_execute sudo snap install rustup --classic
tw_execute sudo snap install authy --beta

tw_log "Installing packages through flatpak..."
tw_execute sudo flatpak install -y \
           org.mozilla.firefox \
           com.getpostman.Postman \
           com.discordapp.Discord \
           com.valvesoftware.Steam
